# TASK-004 — стратегия проверок и фикстур

- Статус: завершено
- Рабочая ветка: `task/004-test-strategy-fixtures`
- Целевая ветка: `main`

## Цель

Зафиксировать воспроизводимую стратегию проверок, точные внешние JSON-документы версии 1, полностью вымышленные статические фикстуры, негативную матрицу и байтовые эталоны первого локального среза без начала программной реализации.

## Включённый объём

- Уровни будущих предметных, контрактных, граничных, интеграционных и byte-determinism проверок.
- Source-specific пакет фиктивного источника и документ критериев `v1`.
- `InputLocation`, категории, коды и стабильный порядок ошибок.
- Небольшой каталог валидных, невалидных, criteria и expected JSON-файлов.
- Golden-результаты с совпадениями, всеми состояниями и успешным пустым результатом.
- Политика версионирования, проверки и контролируемого обновления fixtures.
- Согласование roadmap, checkpoint и реестра задач.

## Исключённый объём

- `pyproject.toml`, `uv.lock`, установка зависимостей и Python-код.
- Pydantic-модели, pytest-тесты, CLI и реализация любого этапа потока.
- База данных, API, HTTP, HTML, реальные площадки, дедупликация и физический объект.
- ИИ, сигналы, уведомления, UI, OpenClaw, Telegram, Docker и развёртывание.
- Программный каркас и следующая задача реализации.

## Критерии готовности

- [x] Оба входных документа v1 описаны без неоднозначности, включая типы, обязательность, extra/null policy и source-specific правила.
- [x] Все статические данные полностью вымышлены, используют только `.example`, фиксированные даты и не требуют сети.
- [x] Матрица покрывает `Present`, `Missing`, `Unsupported`, обязательные негативные случаи и успешное несовпадение.
- [x] Каждая ошибка имеет ожидаемые `category`, `code`, `location`; атомарный отказ запрещает частичный результат.
- [x] Валидный набор проверяет все/часть/отсутствие критериев и устойчивую сортировку.
- [x] Существуют byte-exact результаты с совпадениями, всеми состояниями и пустым массивом.
- [x] Manifest связывает каждый файл со сценарием, уровнем и oracle.
- [x] Стратегия сохраняет независимость ядра от JSON, Pydantic и CLI.
- [x] В репозитории нет исполняемого кода, зависимостей и преждевременной инфраструктуры.
- [x] Все проверки выполнены, задача закоммичена и безопасно интегрирована либо оставлена чистой готовой веткой.

## Фактически выполненная работа

- Создана подробная спецификация `docs/design/TEST-STRATEGY-AND-FIXTURES.md` с девятью уровнями будущих проверок, точными границами и правилами локализации дефектов.
- Зафиксированы source-specific `fixture-source-batch@1`, нейтральный `search-criteria@1`, строгая политика типов/лишних полей/`null`, правила URL, идентификатора, времени, денег, площади и комнат.
- Принято решение 0004 о форме `search-result@1`, стабильном порядке независимых ошибок, byte oracle и контролируемом обновлении golden-файлов.
- Создан `tests/fixtures/v1` с manifest, comprehensive batch, четырьмя criteria, четырьмя ключевыми invalid-документами, тремя успешными golden-результатами и многоошибочным diagnostic oracle.
- Полностью вымышленные записи покрывают `Present`, `Missing`, `Unsupported`, студию `0`, UTC-приведение двух смещений, точное преобразование площади и RUB в копейки.
- Матрица закрепляет ожидаемые `category/code/location`, атомарный отказ и отдельный прямой тест внутреннего `OUTPUT_CONTRACT`.
- `.gitattributes` принудительно сохраняет LF для версионированного fixture-каталога.
- Согласованы архитектура, проект, первый срез, предметная спецификация, README, roadmap, checkpoint и реестры задач.

## Проверки

- Стандартной библиотекой разобраны все 12 корректных JSON-файлов; `invalid/syntax-truncated.json` отдельно подтверждён как синтаксически невалидный.
- Для трёх `search-*.json` подтверждены UTF-8 без BOM, компактный канонический JSON, лексикографические ключи и ровно один завершающий LF.
- Программно сверены порядок совпадений, возрастающий `allowed_rooms`, `Missing` без `raw_value`, `Unsupported` с исходным `USD`, пустой успех и точная последовательность пяти диагностик.
- Проверены только `.example` в fixture URL, отсутствие email/contact/secret-полей и ссылки manifest на существующие файлы.
- Проверены 50 внутренних Markdown-ссылок.
- Подтверждено отсутствие `.py`, `.pyi`, `pyproject.toml`, `uv.lock`, Dockerfile, docker-compose и compose-файлов.
- Выполнены `git diff --check`, `git fsck --full`, проверка `.gitattributes` и просмотр итогового diff.
- После коммита и интеграции повторно проверяются история, SHA и чистота обоих рабочих деревьев.

## Итоговый коммит

Основной содержательный коммит: `26a510847a210a4da96b72d10408db061a6855a4` (`docs: define TASK-004 test strategy and fixtures`). Отдельный следующий metadata-коммит фиксирует этот проверяемый идентификатор в отчёте задачи; интеграция определяется merge-коммитом `merge: complete TASK-004 test strategy and fixtures` в истории `main`.

## Следующая рекомендуемая задача

Отдельная малая задача: создать только программный каркас и единую команду качества, не реализуя входной адаптер, нормализацию, поиск или сквозной CLI.
